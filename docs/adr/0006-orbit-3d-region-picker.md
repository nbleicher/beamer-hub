# The Vehicle is shown as an orbit-able 3D model

The Owner picks Regions by dragging, rotating, and zooming a real 3D render, not a 2D map or a list. Clicks are a separate Region hitbox layer. The mesh is not the parts catalog and will not resolve a trunk bulb or window regulator by itself.

A schematic would have shipped faster and avoided BMW likeness issues. They refused it. v1 still needs a licensed-enough E46 sedan asset; that source is not chosen yet.
