# YouTube search is on the Check Step

The Owner wants search, not a curated watch list. Hits are Suggestions. They become Citations only if the Owner pins one.

This fights least-invasive order: YouTube will surface regulator videos. The query must stay step-shaped (`E46 check window fuse`) so the fuse step does not search “E46 window regulator.” How often we call YouTube is still open; the API documents a small daily `search.list` budget.
