# YouTube search is a button on the Check Step

Search runs when the Owner asks, not when the step opens. Query is `E46` plus the step name. Hits stay Suggestions until pinned.

Automatic search was rejected: it burns the small daily `search.list` budget and shoves regulator videos onto the fuse step.
